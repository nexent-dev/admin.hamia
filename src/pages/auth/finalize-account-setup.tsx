import type { FC } from "react";

const FinalizeAccountSetup: FC = () => {
    return (
        <div>
            <h1>Finalize Account Setup</h1>
        </div>
    );
};

export default FinalizeAccountSetup;
